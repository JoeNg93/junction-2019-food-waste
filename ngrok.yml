# Command: $ ngrok start --config ./ngrok.yml backend frontend
authtoken: YOUR_NGROK_TOKEN
tunnels:
  backend:
    proto: http
    addr: 5001
  frontend:
    proto: http
    addr: 4000
    host-header: localhost:4000
